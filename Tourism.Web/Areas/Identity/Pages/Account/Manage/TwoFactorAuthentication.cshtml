@page
@using Microsoft.AspNetCore.Http.Features
@model TwoFactorAuthenticationModel
@{
    ViewData["Title"] = "Двуфакторна автентикация (2FA)";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<partial name="_StatusMessage" for="StatusMessage" />
<h3>@ViewData["Title"]</h3>
@{
    var consentFeature = HttpContext.Features.Get<ITrackingConsentFeature>();
    @if (consentFeature?.CanTrack ?? true)
    {
        @if (Model.Is2faEnabled)
        {
            if (Model.RecoveryCodesLeft == 0)
            {
                <div class="alert alert-danger">
                    <strong>Нямате останали кодове за възстановяване.</strong>
                    <p>Трябва да <a asp-page="./GenerateRecoveryCodes">генерирате нов набор от кодове</a> преди да влезете с код за възстановяване.</p>
                </div>
            }
            else if (Model.RecoveryCodesLeft == 1)
            {
                <div class="alert alert-danger">
                    <strong>Имате само 1 код за възстановяване.</strong>
                    <p>Можете да <a asp-page="./GenerateRecoveryCodes">генерирате нов набор от кодове</a>.</p>
                </div>
            }
            else if (Model.RecoveryCodesLeft <= 3)
            {
                <div class="alert alert-warning">
                    <strong>Имате @Model.RecoveryCodesLeft кода за възстановяване.</strong>
                    <p>Препоръчваме ви да <a asp-page="./GenerateRecoveryCodes">генерирате нов набор от кодове</a>.</p>
                </div>
            }

            if (Model.IsMachineRemembered)
            {
                <form method="post" style="display: inline-block">
                    <button type="submit" class="btn btn-primary">Забрави този браузър</button>
                </form>
            }
            <a asp-page="./Disable2fa" class="btn btn-primary">Деактивирай 2FA</a>
            <a asp-page="./GenerateRecoveryCodes" class="btn btn-primary">Нулирай кодовете</a>
        }

        <h4>Приложение за автентикация</h4>
        @if (!Model.HasAuthenticator)
        {
            <a id="enable-authenticator" asp-page="./EnableAuthenticator" class="btn btn-primary">Добави приложение</a>
        }
        else
        {
            <a id="enable-authenticator" asp-page="./EnableAuthenticator" class="btn btn-primary">Настрой приложението</a>
            <a id="reset-authenticator" asp-page="./ResetAuthenticator" class="btn btn-primary">Нулирай приложението</a>
        }
    }
    else
    {
        <div class="alert alert-danger">
            <strong>Политиката за поверителност и бисквитки не е приета.</strong>
            <p>Трябва да приемете политиката, преди да активирате двуфакторната автентикация.</p>
        </div>
    }
}

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
